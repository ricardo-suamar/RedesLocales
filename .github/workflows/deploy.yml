name: Deploy Documentation

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Descargar el código del repositorio
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      # 3. Configurar Node.js (necesario para ejecutar StatiCrypt)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 4. Sistema de caché de MkDocs
      - name: Cache MkDocs dependencies
        run: echo "cache_id=$(date --utc +'%Y%V')" >> $GITHUB_ENV

      - name: Restore Cache
        uses: actions/cache@v4
        with:
          key: mkdocs-material-${{ env.cache_id }}
          path: .cache
          restore-keys: |
            mkdocs-material-

      # 5. Instalar dependencias de Python y StatiCrypt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material pymdown-extensions ghp-import
          npm install -g staticrypt

      # 6. Compilar la web con MkDocs
      - name: Build site with MkDocs
        run: mkdocs build -d site

      # 7. Cifrar los HTML compilados pasando la contraseña de forma explícita (-p)
      - name: Encrypt HTML files
        env:
          STATICRYPT_PASSWORD: ${{ secrets.SITIO_PASSWORD }}
        run: |
          staticrypt site -r -d site -p "$STATICRYPT_PASSWORD" --remember 30 --short

      # 8. Configurar bot de Git
      - name: Configure Git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 9. Publicar la versión cifrada a GitHub Pages
      - name: Deploy encrypted site
        run: |
          ghp-import -n -p -f site